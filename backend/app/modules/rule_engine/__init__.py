"""
Module Rule Engine (Sprint 6) — trung tâm Business Logic.

Đọc DetectionResult / TrackingResult / BehaviorFeatureDTO (+ ROI/Timeline/Temporal)
để suy luận hành vi và tạo BehaviorEventDTO. KHÔNG dùng AI model, KHÔNG Telegram,
KHÔNG Database, KHÔNG Dashboard. Mọi Rule cấu hình được (không hardcode).
"""
